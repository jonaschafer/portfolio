export const metadata = {
  title: 'Recovery Map',
  description: 'Post-op recovery reference: where I am, what the rule is today, and when to call the surgeon.',
  robots: { index: false, follow: false },
}

export default function MmaMapLayout({ children }) {
  return children
}
